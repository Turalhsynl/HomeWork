@model List<UserInfo.Entities.User>
@{
}

<h2>Users</h2>
<a href="/Users/Add">Add New User</a>
<ul>
    @foreach (var user in Model)
    {
        <li>
            @user.Name @user.Surname
            <a href="/Users/Details/@user.Id">Details</a>
        </li>
    }
</ul>
